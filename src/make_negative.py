"""Function returns the negative of an input number."""


def make_negative(number):
    """Return negative of number."""
    if number < 0:
        return number
    else:
        return number * -1
